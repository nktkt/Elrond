# Reverse proxy to a single backend.
# Start any HTTP server on 127.0.0.1:3000, then:
#   elrond -c examples/proxy.conf

http {
    server {
        listen 8080;

        location / {
            proxy_pass http://127.0.0.1:3000;
        }
    }
}
